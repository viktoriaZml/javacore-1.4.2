plugins {
    id 'java'
}

group 'ru.netology'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    compile project(":db")
}

jar {
//    libsDirName = '../../jars'
    from {
        configurations.compile.collect { it.isDirectory() ? it : zipTree(it) }
    }
}